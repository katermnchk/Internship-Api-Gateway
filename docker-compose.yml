version: '3.8'

networks:
  innowise-network:
    external: true

services:

  api-gateway:
    build: .
    container_name: api-gateway
    ports:
      - "${GATEWAY_PORT:-8099}:8099"
    networks:
      - innowise-network
